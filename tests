<?php

use PHPUnit\Framework\TestCase;
use App\Calculator;

class CalculatorTest extends TestCase
{
    public function testAdd()
    {
        $c = new Calculator();
        $this->assertEquals(5, $c->add(2, 3));
    }

    public function testSubtract()
    {
        $c = new Calculator();
        $this->assertEquals(1, $c->subtract(3, 2));
    }
}

